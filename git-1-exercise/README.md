# This is an Readme file for Git Exercise




## This file is used for representation of repo changes.

---

To check the status of the current repo we use:

`git status`

---


The add file or folder changes, we have been used

`git add git-1-exercise`

---

To commit those new changes, we used:

`git commit -m "Git folder added"`

---

To push the changes frm the local rpeository to the remote one, we used:

`git push`

---

To create a new brach, w used the next command:

`git branch development`

---

To work in the newest local branch, was used:

`git checkout development`

---

To list all the current branches available in our local repository, we used:

`git branch`
